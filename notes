
Todo:
    Tooling:        
        - Superset? 
            - Setup installation on server
            - Build out some sample datasets / dashboards.
            - write a plan for implementing Superset
        - look at migrating to kubernetes for everything
            - add K9s to local machine for easier monitoring
            x Grafana
            x Superset
            - Airflow

    Database:
        - Run a full back up of dev + prod
        - Take down the prod postgres to enable extend options
        - Setup custom users to stop using admin

    Code changes:
        - operational workflows
            - a set of workflows to add / update operational workflows  
                - coins for coinggecko
                - locations for weather data

    Extensions:
        - Mongo connector
        - Webscrape module

    Grafana:
        - Ingest the dag logs as well
        - Setup dashboard for UFW etc.
        - Prometheus? to monitor the pods etc.
        - Add variables to dashboard to allow for filtering logs on actual failures.

    Testing:
        - Unit tests
            X main
            - connectors
                - file_connector
                - http_connector
                - postgres_connector
                - mongo_connector
        - Integration tests

    Documentation:
        - create docs folder
            - Add spec's / information on each pipeline
        - Interface spec for each connector
            - needs to cover the relevant config for the connector

Pipelines:
    x add: weather sleaford :- http://api.openweathermap.org/data/2.5/weather?id=2641324&appid=
    dota
    liquipedia
    f1
    football
    stocks - https://marketstack.com/signup

    malware
    CVE's

    reference data:
        uk address?
        companies house

    

:: DONE :: 
    X implement dataframes everywhere rather than list[Dict]
    X Make sure that the Common.MapData function is being used
    X Add execution attributes
    X Add json logs
    X Fix Type errors
    X Add export functionality to backup key data
    X Setup ariflow runs for data ingestion
    x Finish the "Staged" workflow system
    X Fix the cron schedule on the ingestion
    x add an id to each execution for easier log travsel 
    X update the existing configs to meet new spec
    x Look at logging to figure out if any changes are necessary
    x logging override
    x Support for complex workflows
    x operational db Support
    X remove silver tables from prod
    X Clean up dev + prod
    x Grafana
    x Investigate the duplication issue.